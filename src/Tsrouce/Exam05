package Exam05

func main() {

  if 조건식 {  //  같은 라인에 있어야 함
    // 조건식이 참일 때
  }
  var tempValue = 100

  if result := 100 * 1; result < tempValue {

  }
  result = 1000  // <-??불가
  // 이유 : 변수의 범위가 if 에서 선언했기 때문에

  if 조건식 {
    // 참일 때
  } else {
    // 조건이 거짓일 일때
  }

  if 조건식 {
    // 참일 때
  } else if 두번째 조건식 {
    // 두번째 조건이 참일 때
  } else if 세번째 조건식 {
    // 세번째 조건이 참일 때
  } else {
    // 모두 거짓일 때
  }

  // 중첩 if 문
  if 조건식 {
    if 조건식 {

    }
  }

  var userID := "aaa"
  var userPW := "1234"
  if userID == //DB결과 값이나 세션에 있는 값 비교 {
    // 참이면
    if userPW == //DB결과 값이나 세션에 있는 값 비교  {
      // 참이면 로그인 성공
    }
    else {
      //로그인 실패 ID 또는 비번 표시
      // 비밀번호를 잘못 입력 했음 로그 기록
    }
  } else {
    //로그인 실패 ID 또는 비번 표시
    // 아이디를 잘못 입력 했음 로그 기록
  }





}
